# 第 3 周 Day 5：协变量偏移与误差累积

[填写本日 Notebook 作业](../notebooks/week-03/day-05.ipynb)

[← 上日：数据划分](day-04.md) · [周首页](README.md) · [次日：Rollout 评估 →](day-06.md)

## 1. 今日目标与预计时间

预计 90–120 分钟。通过小实验观察行为克隆的协变量偏移：训练只见专家状态，但部署访问由自身错误诱导的新状态；能区分一步误差与长期闭环误差。

## 2. 今日知识点及其在 VLA 中的作用

BC 最小化专家状态分布下的动作误差：

\[
\mathbb E_{s\sim d_{expert}}[\ell(\pi(s),a^*)]
\]

部署时状态来自 `d_pi`。微小动作偏差会将机器人带到示范未覆盖区域，之后预测更差，形成误差累积。这解释了为何验证 MSE 很低，VLA 仍可能抓空或越走越偏。

## 3. 必要概念、公式、形状与数据流

一维系统 `x_{t+1}=x_t+a_t`，专家 `a_t=k(x*-x_t)`。训练数据只覆盖理想轨迹附近；给策略加入偏差 `b`：

\[
\hat a_t=a_t^*+b
\]

单步误差是 `|b|`，若没有反馈纠正，T 步位置偏差可随 T 累积。闭环策略可能纠正，但若分布外预测错误随偏离增大，仍会发散。

```text
expert states -> supervised fit
policy error -> new state outside expert support
             -> larger model error -> further drift
```

## 4. 分步骤学习

1. 画 expert/policy 两条状态轨迹。
2. 在同一 expert state 上测 one-step error。
3. 从相同初态 rollout 20 步，记录 state error。
4. 从略有扰动的初态重跑，比较恢复能力。
5. 列举增加数据覆盖、噪声增强、DAgger 类交互收集等缓解方式及成本。

## 5. 必做作业

在 1D/2D 到达环境中构造专家和一个带轻微系统偏差的 imitation policy。报告专家状态上的 action MSE、20 步 rollout 最终误差、最大偏离、成功与否；至少测试 5 个初态，其中两个超出训练初态范围。画图或表格显示误差随时间变化，并用证据解释协变量偏移。

## 6. 输入、预期输出与验证方法

- 输入：专家控制器、训练初态范围、偏差/噪声配置、5 个固定初态。
- 预期：每条轨迹 state `[T+1,d]`、action `[T,d]`；离线与闭环指标同时报告。课程不预设哪组一定失败。
- 验证：专家与策略使用相同动力学和成功标准；训练外初态明确标记；固定 seed；不从 rollout 结果反向修改 test 数据。

低资源替代：在表格中手算一个恒定偏差策略 10 步并比较专家轨迹。

## 7. 提交内容与固定格式

固定包含：`专家/策略定义`、`训练分布`、`离线动作误差`、`5次rollout表`、`误差轨迹`、`协变量偏移解释`、`缓解方案与代价`、`投入分钟数`。

## 8. 评分与验收标准

100 分，80 分通过：实验控制 20；离线指标 15；rollout 指标 25；训练内外比较 20；解释/缓解 20。只提交训练 loss、把专家重新纠正后的状态喂给策略做“rollout”、或混用成功标准均为关键失败。

## 9. 常见错误与排查

- rollout 每步重置到专家状态：这叫 teacher forcing，不是闭环。
- 将环境噪声和策略误差混在一起：先做无噪声对照。
- 只看最终点漏掉中途越界：记录最大偏离/安全事件。
- 失败后缩短 horizon 让结果好看：horizon 是评估协议的一部分。

## 10. 时间不足时的最低完成线

完成一个训练内、一个训练外初态的 10 步对比，并同时报告一步 MSE 与最终误差。

## 11. 可选提高任务

模拟一次 DAgger 迭代：在策略访问状态上查询专家标签，重新拟合后比较；注明现实机器人查询专家的成本与安全约束。

## 12. 完成后的自测题

1. `d_expert` 与 `d_pi` 为什么不同？
2. teacher forcing 指标能替代 rollout 吗？
3. 数据增强怎样帮助，为什么不能保证解决？
4. 如何证明失败来自协变量偏移而非代码时间错位？

[← 上日：数据划分](day-04.md) · [周首页](README.md) · [次日：Rollout 评估 →](day-06.md)
