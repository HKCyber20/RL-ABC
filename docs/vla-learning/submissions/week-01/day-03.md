# Week 01 / Day 03 作业：Q/K/V 与 Scaled Dot-Product Attention

[← Week 01 / Day 02](day-02.md) · [本周作业](README.md) · [对应课程](../../week-01/day-03.md) · [Week 01 / Day 04 →](day-04.md)

> **提交状态：未提交。** 请直接在本文件中填写；不要修改最后的“教练验收区”，也不要自行更新 `PROGRESS.md`。

## 1. 提交信息

| 字段 | 请填写 |
|---|---|
| 实际投入时间 |  |
| 完成日期 |  |
| 执行环境 | OS、Python、PyTorch、CPU/GPU、仿真器；纯理论日写“不适用” |
| 资源等级/替代方案 | L0、L1 或 L2；若使用 mock、synthetic 或 dry-run，请明确 |
| 课程文件 | [打开当天课程](../../week-01/day-03.md) |
| 产物路径 | 代码、日志、图表或截图的相对路径 |

## 2. 今日目标与预计时间

预计 90–120 分钟。能从形状和数值两方面解释 Q、K、V，手写 scaled dot-product attention，并验证每个 query 对所有 key 的权重和为 1。

### 我的目标复述

**我的回答：** 【请在这里用自己的话填写今日目标，以及该能力在 VLA 中的作用。】

## 3. 必做作业

### 课程题目

实现函数 `attention(q,k,v,mask=None)`，不调用框架的整层 attention。使用固定输入：

```python
q = [[[1., 0.], [0., 1.]]]
k = [[[1., 0.], [0., 1.], [1., 1.]]]
v = [[[1., 0.], [0., 2.], [3., 3.]]]
```

提交 scores、weights、output 及形状；断言 `weights.sum(-1)` 接近全 1。再把第三个 key 改为 `[3,0]`，解释第一个 query 权重如何变化。具体小数以实际运行结果为准，不预先伪造。

### 我的答案与实现

> 请保留题号，在每个醒目的作答区中直接填写；可以使用多段文字、表格、公式、Mermaid 或代码块。

#### 第 1 题作答

【请在这里填写第 1 题答案】

#### 第 2 题作答

【请在这里填写第 2 题答案】

#### 第 3 题作答

【请在这里填写第 3 题答案】

## 4. 输入、预期输出与验证

### 课程要求

- 输入形状：Q `[1,2,2]`、K/V `[1,3,2]`。
- 预期形状：scores/weights `[1,2,3]`、output `[1,2,2]`。
- 预期性质：权重非负、最后一维和约为 1、输出有限；修改 K 后至少一个权重发生变化。
- 验证：`assert torch.allclose(weights.sum(-1), ones, atol=1e-6)`；检查 `isfinite`；手算第一行点积用于交叉核对。

低资源替代：用计算器完成上述固定矩阵，softmax 保留三位小数。

### 我的验证记录

| 检查项 | 实际结果 | 证据位置 | 是否符合 |
|---|---|---|---|
| 输入形状或数据格式 |  |  |  |
| 输出形状或数据格式 |  |  |  |
| 数值范围、dtype 或单位 |  |  |  |
| 断言、测试或评估指标 |  |  |  |
| 指标分子/分母与随机种子 |  |  |  |
| checkpoint、配置或数据哈希 |  |  |  |
| 失败分支或反例 |  |  |  |

> 未实际执行的内容必须写“预期结果”，不能写成已运行结果。

## 5. 代码、实验与运行证据

### 产物清单

| 产物 | 路径 | 用途 |
|---|---|---|
| 代码 |  |  |
| 配置 |  |  |
| 日志 |  |  |
| 图表/截图 |  |  |

### 实际运行命令

```text
# 在这里粘贴实际执行的命令；纯理论日写“不适用”
```

### 关键输出

```text
# 粘贴足以验证结果的关键输出，不要只写“运行成功”
```

退出码：

### 结果解释

- 结果说明了什么：
- 结果没有说明什么：
- 与预期不一致的地方：
- 正向测试与负向测试：
- 失败现象和定位证据：
- 可能原因及证据：

## 6. VLA 约束记录

不适用的项目写“不适用”，不要留空。

| 约束 | 本次定义 |
|---|---|
| 图像布局、颜色顺序和范围 |  |
| 文本 token 形状与 padding |  |
| 机器人状态各维含义 |  |
| 坐标系 |  |
| 长度和角度单位 |  |
| 动作空间及各维含义 |  |
| observation/action 时间对齐 |  |
| 控制频率或 action chunk |  |
| 归一化及统计量来源 |  |
| 随机种子与数据划分 |  |

## 7. 当天提交格式要求

提交 Markdown，固定包含：`QKV 直觉`、`形状推导`、`实现`、`原始运行结果`、`修改 key 后对比`、`断言证据`、`投入分钟数`。

提交前确认本文件包含上面要求的全部内容；若课程要求与通用表格不完全一致，以课程要求为准。

## 8. 自测题

### 课程自测

1. 为什么 K 和 V 的序列长度必须相同？
2. Q 与 K 的最后一维为什么必须兼容？
3. 输出序列长度跟随 Q 还是 V？
4. attention 权重是否等于完整的因果解释？为什么不应这样断言？

### 我的自测答案

> 请保留题号，在每个醒目的作答区中直接填写；可以使用多段文字、表格、公式、Mermaid 或代码块。

#### 第 1 题作答

【请在这里填写第 1 题答案】

#### 第 2 题作答

【请在这里填写第 2 题答案】

#### 第 3 题作答

【请在这里填写第 3 题答案】

#### 第 4 题作答

【请在这里填写第 4 题答案】

## 9. 常见错误、最低完成线与可选提高

### 常见错误与排查

- `K.transpose(0,1)` 错换 batch：只交换最后两维。
- 对 query 维 softmax：打印 `weights.sum(-1)` 定位。
- 使用整数张量进入 softmax：转浮点。
- 用 `sqrt(N_k)` 缩放：分母应为 key 的特征维 `sqrt(d_k)`。
- 修改 K 后输出没变：确认变量真正进入重新计算而非复用旧 weights。

### 时间不足时的最低完成线

手算第一行 scores，写出所有形状，并验证一行 softmax 权重和为 1。

### 可选提高任务

比较不缩放与缩放情况下，当 Q/K 同时乘 10 后的权重熵，说明过度尖锐的 softmax 对梯度的影响。

## 10. 提交前自评

- [ ] 我逐项回答了必做作业；
- [ ] 我区分了实际结果与预期结果；
- [ ] 我提供了代码、命令、日志、图表或推理过程作为证据；
- [ ] 我记录了适用的张量形状、坐标系、单位、时间对齐和动作定义；
- [ ] 我运行了当天要求的验证或明确说明未运行原因；
- [ ] 我回答了自测题；
- [ ] 我写出了至少一个仍不确定的点或失败案例。

### 仍不确定的内容或阻塞项

- 现象：
- 最小复现：
- 已尝试：
- 希望获得的帮助：

## 11. 当天评分与验收标准

100 分，80 分通过：公式与缩放 15；形状 20；实现 25；权重行和验证 20；干预实验解释 20。softmax 维度错误、忘记转置 K 或输出不是 `A@V` 属关键项失败。

### 我的自评分

| 项目 | 自评分 | 证据 |
|---|---:|---|
| 概念理解 |  |  |
| 实现或作业正确性 |  |  |
| 验证充分性 |  |  |
| VLA 约束记录 |  |  |
| 分析与表达 |  |  |
| 合计 | /100 |  |

> 自评分只用于检查遗漏，不能代替教练验收。

## 12. 教练验收区

> 本节由教练收到证据后填写。学习者不要预填分数、通过状态或下一课。

| 字段 | 验收结果 |
|---|---|
| 证据完整性 | 待验收 |
| Rubric 得分 | /100 |
| 门槛项 | 待验收 |
| 当天状态 | 未提交 |
| 具体缺口 |  |
| 最小补救任务 |  |
| 复验结果 |  |
| 下一课程 |  |

[← Week 01 / Day 02](day-02.md) · [本周作业](README.md) · [对应课程](../../week-01/day-03.md) · [Week 01 / Day 04 →](day-04.md)
