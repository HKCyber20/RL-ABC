# 第 2 周 Day 2：位姿与齐次变换

[填写本日 Notebook 作业](../notebooks/week-02/day-02.ipynb)

[← 上日：坐标系](day-01.md) · [周首页](README.md) · [次日：旋转表示 →](day-03.md)

## 1. 今日目标与预计时间

预计 90–120 分钟。能用 4×4 齐次矩阵统一表示旋转和平移，正确组合、求逆并验证点从相机系到基座系的变换。

## 2. 今日知识点及其在 VLA 中的作用

机器人观测常来自相机，而控制在基座或末端坐标系执行。齐次变换把多段外参和末端位姿串联起来，是将视觉目标位置转换为动作目标的基本工具；方向写反会让策略向相反位置运动。

## 3. 必要概念、公式、形状与数据流

\[
T^A_B=\begin{bmatrix}R^A_B&t^A_B\\0&1\end{bmatrix},\quad
\bar p^A=T^A_B\bar p^B
\]

链式组合：

\[
T^A_C=T^A_B T^B_C
\]

求逆：

\[
(T^A_B)^{-1}=T^B_A=
\begin{bmatrix}R^T&-R^Tt\\0&1\end{bmatrix}
\]

批量变换可为 `[B,4,4] @ [B,4,1] -> [B,4,1]`。矩阵乘法顺序从右向左作用。

## 4. 分步骤学习

1. 将 Day 1 的 2D 例子扩为 3×3 齐次矩阵。
2. 构造 3D 的 `T^B_C` 和 `T^W_B`，计算 `T^W_C`。
3. 变换一个相机点到世界系。
4. 手推并用代码计算逆矩阵，检查双向恢复。
5. 写 4 个断言检查底行、正交性、组合和 round trip。

## 5. 必做作业

用 NumPy/PyTorch 实现 `make_transform(R,t)`、`invert_transform(T)`、`transform_points(T,p)`。测试 `R_z(90°),t=[1,2,0]` 和三个点 `[0,0,0],[1,0,0],[0,1,0]`；再组合第二个纯平移变换。提交每一步矩阵、点结果和 round-trip 最大误差。

## 6. 输入、预期输出与验证方法

- 输入：上述固定 R/t 和点 `[3,3]`。
- 预期：输出点 `[3,3]`，齐次中间 `[3,4]`；逆变换应恢复输入，浮点误差通常接近 0，具体值以运行结果为准。
- 验证：`T @ inv(T)` 接近 `I4`；恢复误差小于 `1e-6`；最后一行 `[0,0,0,1]`。

低资源替代：只手算原点和 `[1,0,0]` 两点并验证逆向。

## 7. 提交内容与固定格式

固定包含：`符号解释`、`三个函数`、`组合顺序`、`点变换输出`、`round-trip 误差`、`断言日志`、`投入分钟数`。

## 8. 评分与验收标准

100 分，80 分通过：构造 20；组合 20；求逆 20；点变换 20；验证 20。若用 `T^B_C @ T^A_B` 得到 `T^A_C` 或逆的平移写成 `-t`，关键项失败。

## 9. 常见错误与排查

- 行向量/列向量混用：课程公式采用列向量；代码若采用行向量需明确转置约定。
- 忘加齐次 1：位置用 1，方向向量用 0。
- 组合次序颠倒：检查相邻上下标是否能“消去”。
- 用一般逆矩阵掩盖公式错误：同时实现解析逆并对比。

## 10. 时间不足时的最低完成线

写对组合和求逆公式，完成一个点的正向与逆向手算。

## 11. 可选提高任务

实现批量 `[B,N,3]` 点变换，写出广播规则并用循环版本交叉验证。

## 12. 完成后的自测题

1. `T^A_B` 把哪一坐标表达变成哪一表达？
2. 为什么逆矩阵平移是 `-R^Tt`？
3. 方向向量的齐次末位为何是 0？
4. 如何用上下标快速检查组合顺序？

[← 上日：坐标系](day-01.md) · [周首页](README.md) · [次日：旋转表示 →](day-03.md)
